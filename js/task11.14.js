var five=5;

for (var x = 1; x <= 10; x++) {
var five=5;
document.write( `${five} x ${x}= ${five * x}` + "<br>"); }

// ${} is a placeholder that allows values ​​or expressions to be inserted into texts.
// This is for clarification, Coach Leen*_*